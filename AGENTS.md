# ReadingSteiner — AI 协作约定

## 强制规则：计划文件必须与代码同步

1. **开工前**：先读 `PLAN.md`，找到“当前状态”和第一个未勾选（`[ ]`）的任务，从那里继续。
2. **完工后**：立即更新 `PLAN.md`：
   - 完成的 `[ ]` 改为 `[x]`；
   - 在文末 `## 进度日志` 追加一行：日期、完成内容、改动文件、验证方式；
   - 更新 `## 16. 当前状态` 与阶段说明。
3. **计划变更**：任何需求、架构、技术选型或接口变化，先修改 `PLAN.md` 对应章节并写明原因，再写代码。
4. **不得跳过**：一次推进结束后若 PLAN.md 仍是旧的，视为任务未完成。
5. **提交**：PLAN.md 有变化时，和代码一起提交。

## 项目事实

- 项目名：ReadingSteiner（Rust）。
- 交互：CLI + TUI，无 Web。
- 部署：NixOS flake 直接引用。
- camofox 是可选抓取引擎：通过其 HTTP API 调用（契约文件 `camofox-openapi.json`），不内置、不捆绑。
- 推送渠道：仅 Telegram Bot。
